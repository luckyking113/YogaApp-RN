import React from 'react';

import { Switch, SafeAreaView, View, FlatList, Text, Image } from 'react-native';
import Icon from 'react-native-vector-icons/AntDesign';
import styles from './BusinessCustomStyle';
import { TouchableOpacity, ScrollView } from 'react-native-gesture-handler';
import Colors from '../../Theme/Colors';

export default class OutMoneyScreen extends React.Component {
  render() {
    return (
      <View style={styles.container}>
        <Text>Need to deliver the Detail Requirements</Text>
      </View>
    )
  }
}
